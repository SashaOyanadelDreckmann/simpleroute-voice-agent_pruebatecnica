{
  "name": "simpleroute-voice-agent",
  "private": true,
  "version": "0.0.1",
  "packageManager": "pnpm@9.12.3",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "pnpm -r --filter ./apps/** build",
    "start": "pnpm --filter ./apps/api start"
  }
}