# Backend (apps/api)

agent/ → cerebro del agente
voice/ → pipeline de voz (chained)
db/ → datos simulados
routes/ → API para frontend
types/ → contratos

# Frontend (apps/web)

components/ → UI de llamada + tablas
lib/api.ts → cliente backend
stores/ → estado de llamada (tipo agente)